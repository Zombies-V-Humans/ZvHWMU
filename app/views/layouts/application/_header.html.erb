    <nav class="navbar navbar-default" id="TheHeader">
        <div class="container-fluid">
            <div class="navbar-header">
             <% if user_signed_in? %>
         <a class="navbar-toggle" id="navbarpic" type="button" data-target="#baseapp-navbar-collapse-1" data-toggle="collapse" id="navbarbutton">
         <%= image_tag(current_user.avatar)%>
        </a>
      <%else%>
        <button class="navbar-toggle" data-target="#baseapp-navbar-collapse-1" data-toggle="collapse" id="navbarbutton" type="button" style="padding-top:15px !important; padding-bottom:15px!important;">
        <span class="sr-only">Toggle navigation</span> 
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        </button>   
          <% end %>
            <% if user_signed_in? %>
                <% if current_page?('/zombie_groups')|| current_page?('/comments/new')|| current_page?('/tasks') %>
                   <%= render "layouts/application/_player_buttons" %> 
                <% end %>
                <% else %>
                   <% if current_page?('/users/sign_in') %> 
                   <% else %>
                      <%= link_to "Sign in", new_user_session_path, :id => "signInHome", :class => "navbar-toggle" %>
                   <% end %>
                <% end -%>

                <%= link_to "WMU ZVH", root_path, :class => "navbar-brand", :id => "brand" %>
            </div>

            <div class="collapse navbar-collapse" id=
            "baseapp-navbar-collapse-1">
                <ul class="nav navbar-nav">
                <li class="navbutton">
                      <%= link_to "Home", '/tasks'%>
                    </li>
                    <li class="navbutton video" >
                      <%= link_to "New! Promotional Video!", '/pages/video', :style=>"color:rgb(255, 158, 0)!important;"%>
                    </li>
                     <li class="navbutton">
                      <%= link_to "The Rules", '/pages/rules'%>
                    </li>
                  <% if user_signed_in? %>
                     <% if current_user.admin? %>
                    <li class="navbutton">
                       <%= link_to "Admin", admin_path %>
                    </li>
                     <% end %>
                    <li class="navbutton">
                       <%= link_to "Account settings", edit_user_registration_path %>
                    </li>
                    <li class="navbutton">
                        <%= link_to "Sign out", destroy_user_session_path %>
                    </li>
               <% end -%>
                </ul>
            </div>
                        <div class="collapse navbar-collapse" id=
            "baseapp-navbar-collapse-2">
                <ul class="nav navbar-nav">
                    <li class="navbutton">
                      <%= link_to "Tag a Zombie", '/tasks'%>
                    </li>
                  
                    <li class="navbutton">
                       <%= link_to "Zombie Map", '/zombie_groups' %>
                    </li>
                    <li class="navbutton">
                        <%= link_to "Global Chat", "/comments/new" %>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
