<div id="chat"  class="collapse" >
  <div class="well">
    <%= form_for @comment, remote: true do |f| %>
      <div class="form-group">
        <%= f.label :body, 'Global Chat' %>
          <%= f.text_area :body, rows: 3, class: 'form-control', required: true, maxlength: 600 %>
            <small class="label label-warning">
              Cannot be blank or contain more than 600 symbols.
            </small>
          </div>
          <%= f.submit 'Post', class: 'btn btn-primary btn-lg' %>
    <% end %>
  </div>
            
     <div class="panel panel-default" id="comments">
       <div class="panel-body">
         <ul id = "acomment" class="media-list">
           <%= render @comments %>
           </ul>
       </div>
     </div>
</div>
          <%= render "layouts/application/_map" %>