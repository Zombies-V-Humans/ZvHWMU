  <div class="form-group">
    <div class="col-sm-offset-2 ">
<div class="page-header">
  <h2>Sign in</h2></div>
</div>
</div>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class => "form-horizontal", :role => "form" }) do |f| %>

  <div class="form-group">
    <%= f.label :email, :class => "col-sm-2 control-label" %>
    <div class="col-sm-4">
      <%= f.email_field :email, :type => :email, :class => "form-control", :placeholder => "Enter your email" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :password, :class => "col-sm-2 control-label" %>
    <div class="col-sm-4">
      <%= f.password_field :password, :class => "form-control", :placeholder => "Password" %>
    </div>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-4">
        <div class="checkbox">
          <%= f.label :remember_me do %>
            <%= f.check_box :remember_me %>
            Remember me
          <% end -%>
        </div>
      </div>
    </div>
  <% end -%>

    <%= f.label :signup, :class => "col-sm-2 control-label", :id => "hideme" %>
  <div class="form-group">
    <div class="col-sm-4">
      <button type="submit" id="regsignup" class="btn btn-default">Sign in</button>
  <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) , id: "fbsignup", class: "btn btn-default" %>
    </div>
  </div>
<% end %>

<div class="row top48">
  <div class="col-sm-offset-2 col-sm-4">
    <%= render "devise/shared/links" %>
  </div>
</div>
